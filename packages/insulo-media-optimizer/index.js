/**
 * Build a custom handler based upon this, calling `run_internal` from the handler for automatic prettify
 *
 * @type {HandlerBase}
 */
module.exports.HandlerBase = require('./lib/HandlerBase');

/**
 * @type {MediaOptimizer}
 */
module.exports.MediaOptimizer = require('./lib/MediaOptimizer');